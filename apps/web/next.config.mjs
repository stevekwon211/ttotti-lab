/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [
    "@ttotti/ui",
    "@ttotti/vision",
    "@ttotti/effects",
    "@ttotti/hand-particles",
  ],
}

export default nextConfig
